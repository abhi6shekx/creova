(() => {
  const left = document.querySelector('.left');
  const trigger = document.querySelector('#storeWorkspaceBtn');
  const ownerTrigger = document.querySelector('#ownerDashboardBtn');
  if (!left || !trigger) return;
  const style = document.createElement('style');
  style.textContent = `.store-workspace{display:none;flex:1;min-height:0;flex-direction:column}.store-workspace.open{display:flex}.store-search{margin:8px 11px 12px;padding:10px;border:1px solid var(--line);border-radius:9px;background:#0f1219;color:#fff}.store-scroll{overflow:auto;padding:0 11px 14px}.store-label{font-size:10px;letter-spacing:.16em;color:#747b8b;margin:15px 8px 7px}.store-owner-note{margin:4px 11px 10px;padding:9px 10px;border:1px solid #322567;border-radius:9px;background:#17122c;color:#c9bbff;font-size:10px;line-height:1.4}.store-item{width:100%;border:0;background:transparent;color:#c9ced8;padding:10px;border-radius:9px;text-align:left;display:flex;justify-content:space-between;align-items:center;gap:8px}.store-item:hover,.store-item.active{background:var(--soft);color:#fff;box-shadow:inset 3px 0 0 var(--accent)}.store-item-main{display:flex;align-items:center;gap:9px}.store-count{font-size:10px;color:#a4aabd;background:#1a1f29;border-radius:999px;padding:3px 7px}.store-back{margin:2px 11px 8px}`;
  document.head.append(style);
  const visibilityStyle = document.createElement('style');
  visibilityStyle.textContent = '.left>.hidden{display:none!important}';
  document.head.append(visibilityStyle);
  const featureStyle = document.createElement('style');
  featureStyle.textContent = `.canvas.hidden{display:none!important}.canvas-wrap:has(.store-feature.open){display:block;overflow:auto;padding:0 8px}.store-feature{display:none;width:min(1100px,100%);min-height:100%;margin:0 auto 20px;padding:18px;color:#eef1f7}.store-feature.open{display:block}.store-feature-head{display:flex;justify-content:space-between;align-items:end;gap:16px;margin:14px 0}.store-feature-head h2{margin:0;font:750 28px Manrope}.store-feature-head p{margin:5px 0 0;color:#9fa7b7}.store-action{border:1px solid #825cff;background:#7651ff;color:#fff;border-radius:9px;padding:10px 13px;font-weight:700}.store-card,.store-table{border:1px solid var(--line);border-radius:12px;background:#0d1016;overflow:hidden}.store-table{width:100%;border-collapse:collapse}.store-table th,.store-table td{padding:13px 15px;border-bottom:1px solid var(--line);text-align:left;font-size:13px}.store-table th{color:#9099aa;font-size:10px;letter-spacing:.1em}.store-table tr:last-child td{border-bottom:0}.store-product{display:flex;align-items:center;gap:11px}.store-product img{width:43px;height:51px;border-radius:7px;object-fit:cover;background:#1b202b}.store-product b{display:block}.store-product small{display:block;color:#949cac;margin-top:3px}.store-pill{font-size:10px;padding:4px 7px;border-radius:999px;background:#173c31;color:#70e4b1}.store-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.store-card{padding:18px}.store-card b{font:700 22px Manrope}.store-card small{display:block;color:#969eae;margin-top:5px}.store-form{display:grid;grid-template-columns:1fr 1fr;gap:12px;padding:18px}.store-form input,.store-form select{width:100%;padding:10px;border:1px solid var(--line);border-radius:8px;background:#11151d;color:#fff}.store-notice{margin-top:12px;color:#78e6b4;font-size:12px}.access-banner{padding:13px 15px;border:1px solid #45307f;border-radius:12px;background:#161126;color:#d8caff;line-height:1.55;margin-bottom:15px}.access-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.access-card{padding:16px;border:1px solid var(--line);border-radius:12px;background:#0d1016}.access-card h3{margin:0 0 5px;font-size:14px}.access-card p{margin:0;color:#9fa7b7;font-size:12px;line-height:1.5}.access-row{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:12px 0;border-bottom:1px solid var(--line)}.access-row:last-child{border-bottom:0}.access-switch{appearance:none;width:38px;height:22px;border-radius:99px;background:#3a4050;position:relative;cursor:pointer;transition:.2s}.access-switch:after{content:'';width:16px;height:16px;border-radius:50%;background:#fff;position:absolute;left:3px;top:3px;transition:.2s}.access-switch:checked{background:#7651ff}.access-switch:checked:after{transform:translateX(16px)}.plan-options{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.plan-option{padding:16px;border:1px solid var(--line);border-radius:12px;background:#0d1016;cursor:pointer}.plan-option.selected{border-color:#825cff;background:#15102a}.plan-option strong{display:block;font-size:16px}.plan-option small{display:block;color:#9fa7b7;line-height:1.45;margin-top:7px}.site-address{font:700 22px Manrope;color:#d9ceff;margin:8px 0}.plan-field{margin:16px 0}.plan-field label{display:block;font-size:11px;color:#9fa7b7;margin-bottom:6px}.plan-field input{width:100%;padding:11px;border:1px solid var(--line);border-radius:9px;background:#11151d;color:#fff}@media(max-width:750px){.store-grid,.access-grid,.plan-options{grid-template-columns:1fr}.store-form{grid-template-columns:1fr}}`;
  document.head.append(featureStyle);
  const productActionStyle = document.createElement('style');
  productActionStyle.textContent = '.store-remove{border:1px solid #4a3038;background:transparent;color:#ffb7c2;border-radius:7px;padding:7px 9px;font-size:11px;cursor:pointer}.store-remove:hover{background:#4a1820}';
  document.head.append(productActionStyle);
  const panel = document.createElement('section');
  panel.className = 'store-workspace';
  panel.innerHTML = `<button class="nav-item store-back"><i data-lucide="layout-panel-top" class="icon"></i>Website editor</button><input class="store-search" placeholder="Search store..."><div class="store-scroll"><div class="store-label">CATALOG</div><button class="store-item active"><span class="store-item-main"><i data-lucide="package" class="icon-sm"></i>Products</span><span class="store-count">32</span></button><button class="store-item"><span class="store-item-main"><i data-lucide="folders" class="icon-sm"></i>Collections</span><span class="store-count">6</span></button><button class="store-item"><span class="store-item-main"><i data-lucide="boxes" class="icon-sm"></i>Inventory</span></button></div>`;
  left.append(panel);
  const ownerPanel = document.createElement('section');
  ownerPanel.className = 'store-workspace';
  ownerPanel.innerHTML = `<button class="nav-item store-back"><i data-lucide="layout-panel-top" class="icon"></i>Website editor</button><div class="store-owner-note"><b>OWNER DASHBOARD</b><br>Private business controls. This is never part of the customer website.</div><div class="store-scroll"><div class="store-label">ORDERS & SELLING</div><button class="store-item active"><span class="store-item-main"><i data-lucide="receipt" class="icon-sm"></i>Orders</span><span class="store-count">12</span></button><button class="store-item"><span class="store-item-main"><i data-lucide="badge-percent" class="icon-sm"></i>Discounts</span></button><button class="store-item"><span class="store-item-main"><i data-lucide="credit-card" class="icon-sm"></i>Payments</span></button><button class="store-item"><span class="store-item-main"><i data-lucide="truck" class="icon-sm"></i>Shipping</span></button><button class="store-item"><span class="store-item-main"><i data-lucide="undo-2" class="icon-sm"></i>Returns</span></button><button class="store-item"><span class="store-item-main"><i data-lucide="users" class="icon-sm"></i>Customers</span></button><div class="store-label">CUSTOMER EXPERIENCE</div><button class="store-item"><span class="store-item-main"><i data-lucide="user-round-cog" class="icon-sm"></i>Customer access</span></button><button class="store-item"><span class="store-item-main"><i data-lucide="globe-2" class="icon-sm"></i>Site plan & address</span></button></div>`;
  left.append(ownerPanel);
  const canvasWrap = document.querySelector('.canvas-wrap');
  const canvas = document.querySelector('.canvas');
  const feature = document.createElement('section');
  feature.className = 'store-feature';
  canvasWrap?.append(feature);
  const views = {
    Products: {desc:'Manage the products customers see in your storefront.',action:'Add product',products:[['Wool Overcoat','Outerwear · SKU 1001','₹22,900','https://images.unsplash.com/photo-1539533018447-63fcce2678e3?auto=format&fit=crop&w=180&q=80'],['Linen Shirt','Shirts · SKU 1002','₹6,900','https://images.unsplash.com/photo-1602810318383-e386cc2a3ccf?auto=format&fit=crop&w=180&q=80'],['Merino Knit','Knitwear · SKU 1003','₹8,500','https://images.unsplash.com/photo-1610652492500-ded49ceeb378?auto=format&fit=crop&w=180&q=80']]},
    Collections:{desc:'Group products into curated shopping collections.',action:'New collection',rows:[['Best Sellers','12 products','Live'],['New Arrivals','8 products','Live'],['Winter Essentials','6 products','Draft']]},
    Inventory:{desc:'Track stock levels and availability across your catalogue.',action:'Adjust stock',rows:[['Wool Overcoat','24 units','Healthy'],['Merino Knit','4 units','Low stock'],['Court Shoe','0 units','Out of stock']]},
    Orders:{desc:'Review, fulfill and manage recent customer orders.',action:'Create order',rows:[['#1048','Aarav Singh','₹22,900','Paid'],['#1047','Maya Shah','₹8,500','Processing'],['#1046','Neel Patel','₹6,900','Shipped']]},
    Discounts:{desc:'Create promotions and manage active discount codes.',action:'Create discount',rows:[['WELCOME10','10% off','Active'],['SUMMER25','25% off orders over ₹5,000','Active'],['VIP15','15% off','Scheduled']]},
    Payments:{desc:'See sales, payouts and payment method settings.',action:'View payouts',cards:[['₹1,24,800','Available balance'],['₹38,450','Next payout'],['₹3,84,200','Total sales']]},
    Shipping:{desc:'Set delivery zones, rates and fulfillment rules.',action:'Add shipping zone',rows:[['India','Standard · 3–5 days','₹99'],['India','Express · 1–2 days','₹249'],['International','DHL · 5–10 days','Calculated']]},
    Returns:{desc:'Review return requests and keep customers informed.',action:'Return policy',rows:[['#1040','Size exchange','Requested'],['#1033','Damaged item','Approved'],['#1028','Changed mind','Refunded']]},
    Customers:{desc:'Understand customers and build stronger repeat relationships.',action:'Add customer',rows:[['Aarav Singh','3 orders','₹38,300'],['Maya Shah','2 orders','₹17,400'],['Neel Patel','1 order','₹6,900']]}
  };
  const catalogKey = `creova_store_catalog_${localStorage.getItem('creova_experience_category')||'general'}_${localStorage.getItem('creova_experience')||'default'}`.replace(/[^a-z0-9_]/gi,'_').toLowerCase();
  const defaultCatalog = [
    {id:'wool-blend-overcoat',name:'Wool Blend Overcoat',category:'Outerwear',price:22900,image:'https://images.unsplash.com/photo-1591047139829-d91aecb6caea?auto=format&fit=crop&w=180&q=86'},
    {id:'relaxed-linen-shirt',name:'Relaxed Linen Shirt',category:'Shirts',price:6900,image:'https://images.unsplash.com/photo-1598033129183-c4f50c736f10?auto=format&fit=crop&w=180&q=86'},
    {id:'merino-crew-knit',name:'Merino Crew Knit',category:'Knitwear',price:8500,image:'https://images.unsplash.com/photo-1576566588028-4147f3842f27?auto=format&fit=crop&w=180&q=86'},
    {id:'leather-court-shoe',name:'Leather Court Shoe',category:'Accessories',price:11900,image:'https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&w=180&q=86'}
  ];
  const storedCatalog=localStorage.getItem(catalogKey); let catalog=[];
  if(storedCatalog===null){catalog=defaultCatalog;localStorage.setItem(catalogKey,JSON.stringify(catalog));}else{try{catalog=JSON.parse(storedCatalog);}catch(_){catalog=defaultCatalog;localStorage.setItem(catalogKey,JSON.stringify(catalog));}if(!Array.isArray(catalog))catalog=[];}
  const addedProductsKey=`creova_added_products_${(localStorage.getItem('creova_experience')||'default').replace(/[^a-z0-9]/gi,'_')}`;
  if(!catalog.length){
    try{
      const added=JSON.parse(localStorage.getItem(addedProductsKey)||'[]');
      const scandinavian=JSON.parse(localStorage.getItem('creova_scandi_products')||'[]');
      const restored=[...(Array.isArray(added)?added:[]).map((product,index)=>({id:`restored-${index}-${product.name}`,name:product.name,price:product.price,category:product.category||'New Arrivals',stock:product.stock??10,image:product.image})),...(Array.isArray(scandinavian)?scandinavian:[]).filter(product=>Array.isArray(product)).map((product,index)=>({id:`scandi-${index}-${product[0]}`,name:product[0],price:Number(String(product[1]).replace(/[^0-9]/g,'')),category:product[2]||'New Arrivals',stock:10,image:product[6]}))];
      if(restored.length){
        catalog=restored;
        localStorage.setItem(catalogKey,JSON.stringify(catalog));
      }
    }catch(_){}
  }
  const saveCatalog=()=>localStorage.setItem(catalogKey,JSON.stringify(catalog));
  const renderProducts=()=>{
    panel.querySelector('.store-item.active .store-count').textContent=String(catalog.length);
    const body=catalog.length?`<table class="store-table"><thead><tr><th>PRODUCT</th><th>CATEGORY</th><th>PRICE</th><th>STATUS</th><th></th></tr></thead><tbody>${catalog.map(product=>`<tr><td><div class="store-product"><img src="${product.image}" alt=""><span><b>${product.name}</b><small>${product.category} · ${product.stock??10} in stock</small></span></div></td><td>${product.category}</td><td>₹${Number(product.price).toLocaleString('en-IN')}</td><td><span class="store-pill">In stock</span></td><td><button class="store-remove" data-remove-product="${product.id}">Remove</button></td></tr>`).join('')}</tbody></table>`:`<div class="store-card" style="text-align:center;padding:52px 20px"><b style="font-size:18px">No products yet</b><small>Add your first product to start selling on this website.</small></div>`;
    feature.innerHTML=`<div class="store-feature-head"><div><h2>Products</h2><p>Manage the products customers see in your storefront.</p></div><button class="store-action" id="storeAddProduct">Add product</button></div>${body}<div class="store-notice" id="productNotice" aria-live="polite"></div>`;
    feature.querySelector('#storeAddProduct').onclick=()=>{document.querySelector('#productModal')?.classList.add('open');window.lucide?.createIcons();};
    feature.querySelectorAll('[data-remove-product]').forEach(button=>button.onclick=()=>{const product=catalog.find(item=>item.id===button.dataset.removeProduct);catalog=catalog.filter(item=>item.id!==button.dataset.removeProduct);saveCatalog();window.dispatchEvent(new CustomEvent('creova:product-removed',{detail:product}));renderProducts();feature.querySelector('#productNotice').textContent=`${product.name} removed from this website.`;});
  };
  const collectionRules={
    'Best Sellers':()=>catalog,
    'New Arrivals':()=>catalog.filter(product=>product.category==='New Arrivals'||product.category==='Accessories'),
    'Winter Essentials':()=>catalog.filter(product=>['Outerwear','Knitwear'].includes(product.category))
  };
  const renderCollectionDetail=name=>{
    const products=(collectionRules[name]||(()=>[]))();
    const body=products.length?`<div class="store-grid">${products.map(product=>`<article class="store-card"><div class="store-product"><img src="${product.image}" alt="${product.name}"><span><b>${product.name}</b><small>${product.category}</small><small>₹${Number(product.price).toLocaleString('en-IN')}</small></span></div></article>`).join('')}</div>`:`<div class="store-card" style="text-align:center;padding:52px 20px"><b style="font-size:18px">No products in this collection</b><small>Add products first, then assign them to ${name}.</small></div>`;
    feature.innerHTML=`<div class="store-feature-head"><div><button class="store-remove" id="backToCollections" style="margin-bottom:12px">← Back to collections</button><h2>${name}</h2><p>Products currently included in this collection.</p></div><button class="store-action" id="openProductManager">Manage products</button></div>${body}`;
    feature.querySelector('#backToCollections').onclick=renderCollections;
    feature.querySelector('#openProductManager').onclick=renderProducts;
  };
  const renderCollections=()=>{
    const rows=Object.keys(collectionRules).map(name=>({name,count:(collectionRules[name]||(()=>[]))().length,status:name==='Winter Essentials'?'Draft':'Live'}));
    panel.querySelectorAll('.store-item').forEach(item=>{if(item.textContent.includes('Collections'))item.querySelector('.store-count').textContent=String(rows.length)});
    feature.innerHTML=`<div class="store-feature-head"><div><h2>Collections</h2><p>Group products into curated shopping collections.</p></div><button class="store-action" id="newCollection">New collection</button></div><table class="store-table"><thead><tr><th>NAME</th><th>PRODUCTS</th><th>STATUS</th><th></th></tr></thead><tbody>${rows.map(row=>`<tr data-collection="${row.name}" style="cursor:pointer"><td><b>${row.name}</b></td><td>${row.count} product${row.count===1?'':'s'}</td><td><span class="store-pill">${row.status}</span></td><td>→</td></tr>`).join('')}</tbody></table><div class="store-notice" id="collectionNotice" aria-live="polite"></div>`;
    feature.querySelectorAll('[data-collection]').forEach(row=>row.onclick=()=>renderCollectionDetail(row.dataset.collection));
    feature.querySelector('#newCollection').onclick=()=>feature.querySelector('#collectionNotice').textContent='New collection setup will let you choose a name and products.';
  };
  const customerAccess = () => {
    const rawCategory = (localStorage.getItem('creova_experience_category') || 'business').toLowerCase();
    const category = rawCategory.replace(/\s+/g, '');
    const recommended = {
      fashion:['Optional','Orders, addresses, wishlist and returns'], store:['Optional','Orders, addresses, wishlist and returns'], salon:['Optional','Bookings, rescheduling and visit history'], fitness:['Recommended','Memberships, class bookings and progress'], restaurant:['Optional','Reservations, preferences and loyalty'], doctor:['Recommended','Appointments, records and reports'], education:['Recommended','Courses, lessons and certificates'], portfolio:['Usually not needed','Contact form only'], business:['Usually not needed','Lead and contact form'], realestate:['Optional','Saved properties and enquiries'], ai:['Required','Product dashboard and subscriptions'], gaming:['Recommended','Player profile, community and purchases'], travel:['Optional','Trips, bookings and saved itineraries']
    }[category] || ['Optional','Customer portal features for this experience'];
    const key = `creova_customer_access_${category}`;
    let settings = { accounts: recommended[0] === 'Recommended' || recommended[0] === 'Required', google: true, otp: true, bookings: ['salon','restaurant','doctor','fitness','travel'].includes(category), memberships: ['fitness','education','ai'].includes(category), portal: ['fashion','store','salon','fitness','restaurant','doctor','education','realestate','ai','gaming','travel'].includes(category) };
    try { settings = { ...settings, ...JSON.parse(localStorage.getItem(key) || '{}') }; } catch (_) {}
    const toggle = (id,label,text) => `<div class="access-row"><div><b>${label}</b><p>${text}</p></div><input class="access-switch" type="checkbox" data-access="${id}" ${settings[id] ? 'checked' : ''}></div>`;
    feature.innerHTML = `<div class="store-feature-head"><div><h2>Customer access</h2><p>Choose what customers can do on your published website.</p></div><button class="store-action" id="saveCustomerAccess">Save settings</button></div><div class="access-banner"><b>Brand owner only</b><br>These controls are private to your dashboard. Website visitors only see the login, booking or account features you enable.</div><div class="access-grid"><section class="access-card"><h3>For this ${rawCategory} experience</h3><p><b>${recommended[0]}</b> — ${recommended[1]}</p>${toggle('accounts','Enable customer accounts','Show sign up, login and customer profile pages.')}${toggle('portal','Customer portal','Let signed-in customers see their personal activity.')}</section><section class="access-card"><h3>Customer authentication</h3><p>Choose the easiest ways for customers to sign in.</p>${toggle('google','Continue with Google','One-tap Google sign-in on the public website.')}${toggle('otp','Email or phone OTP','Passwordless code-based login.')}</section><section class="access-card"><h3>Business features</h3><p>Turn these on only when this brand needs them.</p>${toggle('bookings','Online bookings','Bookings, reschedule/cancel and appointment history.')}${toggle('memberships','Memberships & subscriptions','Plans, paid member access and renewal status.')}</section><section class="access-card"><h3>Customer data isolation</h3><p>Every customer account belongs only to this brand.</p><div class="store-notice">Customers cannot access another Creova brand’s orders, bookings or subscriptions.</div></section></div><div class="store-notice" id="accessNotice" aria-live="polite"></div>`;
    feature.querySelector('#saveCustomerAccess').onclick = () => { const next = {}; feature.querySelectorAll('[data-access]').forEach(input => next[input.dataset.access] = input.checked); localStorage.setItem(key, JSON.stringify(next)); feature.querySelector('#accessNotice').textContent = 'Customer access settings saved for this brand.'; };
  };
  const sitePlan = () => {
    const key = 'creova_site_plan';
    let state = { plan:'free', handle:'yourbrand', customDomain:'' };
    try { state={...state,...JSON.parse(localStorage.getItem(key)||'{}')}; } catch (_) {}
    const plan = (id,title,price,detail) => `<button class="plan-option ${state.plan===id?'selected':''}" data-plan="${id}"><strong>${title}</strong><small>${price}<br>${detail}</small></button>`;
    const address = state.plan==='starter' && state.customDomain ? state.customDomain.replace(/^https?:\/\//,'') : `${state.handle || 'yourbrand'}.thecreova.com`;
    feature.innerHTML = `<div class="store-feature-head"><div><h2>Site plan & address</h2><p>Choose how this website is published and branded.</p></div><button class="store-action" id="saveSitePlan">Save changes</button></div><div class="access-banner"><b>Free publishing is ready</b><br>Your customer can publish on a Creova subdomain immediately. A custom domain always remains owned by the brand owner.</div><section class="access-card"><h3>Website address</h3><div class="site-address" id="siteAddress">${address}</div><div class="plan-field"><label>Creova site handle</label><input id="siteHandle" value="${state.handle}" maxlength="32" placeholder="yourbrand"></div><div class="plan-field"><label>Custom domain (Starter plan)</label><input id="customDomain" value="${state.customDomain}" placeholder="www.yourbrand.com"></div></section><div class="plan-options">${plan('free','Free','₹0 / month','thecreova.com address + Creova footer promotion')}${plan('clean','Clean','₹149 / month','Remove Creova promotion; keep Creova address')}${plan('starter','Starter','₹499 / month','Custom domain + no branding + more store tools')}</div><section class="access-card" style="margin-top:12px"><h3>Free plan promotion</h3><p>Free sites show a respectful footer badge: “Built with Creova”. No intrusive pop-ups or auto-play ads.</p><div class="store-notice">Choose Clean to remove this branding, or Starter to connect a custom domain.</div></section><div class="store-notice" id="planNotice" aria-live="polite"></div>`;
    feature.querySelectorAll('[data-plan]').forEach(button => button.onclick=()=>{state.plan=button.dataset.plan;feature.querySelectorAll('[data-plan]').forEach(item=>item.classList.toggle('selected',item===button));});
    feature.querySelector('#saveSitePlan').onclick=()=>{state.handle=feature.querySelector('#siteHandle').value.trim().toLowerCase().replace(/[^a-z0-9-]/g,'-').replace(/^-+|-+$/g,'')||'yourbrand';state.customDomain=feature.querySelector('#customDomain').value.trim();localStorage.setItem(key,JSON.stringify(state));feature.querySelector('#siteAddress').textContent=state.plan==='starter'&&state.customDomain?state.customDomain.replace(/^https?:\/\//,''):`${state.handle}.thecreova.com`;feature.querySelector('#planNotice').textContent='Publishing plan and website address saved.';};
  };
  function renderFeature(name='Products') { if(name === 'Products') return renderProducts(); if(name === 'Collections') return renderCollections(); if(name === 'Customer access') return customerAccess(); if(name === 'Site plan & address') return sitePlan(); const v=views[name]||views.Products; const content=v.cards?`<div class="store-grid">${v.cards.map(c=>`<article class="store-card"><b>${c[0]}</b><small>${c[1]}</small></article>`).join('')}</div>`:`<table class="store-table"><thead><tr>${v.rows[0].map((_,i)=>`<th>${['NAME','DETAILS','AMOUNT / STATUS','STATUS'][i]}</th>`).join('')}</tr></thead><tbody>${v.rows.map(r=>`<tr>${r.map((x,i)=>`<td>${i===r.length-1?`<span class="store-pill">${x}</span>`:x}</td>`).join('')}</tr>`).join('')}</tbody></table>`; feature.innerHTML=`<div class="store-feature-head"><div><h2>${name}</h2><p>${v.desc}</p></div><button class="store-action">${v.action}</button></div>${content}<div class="store-notice" aria-live="polite"></div>`; feature.querySelector('.store-action').onclick=()=>feature.querySelector('.store-notice').textContent=`${v.action} flow opened.`; }
  const websiteNodes = [...left.children].filter(node => node !== panel && node !== ownerPanel);
  const setWorkspace = mode => { const open = Boolean(mode); websiteNodes.forEach(node => node.classList.toggle('hidden', open)); panel.classList.toggle('open', mode === 'store'); ownerPanel.classList.toggle('open', mode === 'owner'); trigger.classList.toggle('active', mode === 'store'); ownerTrigger?.classList.toggle('active', mode === 'owner'); canvas?.classList.toggle('hidden', open); feature?.classList.toggle('open', open); if(mode) renderFeature(mode === 'owner' ? 'Orders' : 'Products'); window.lucide?.createIcons(); };
  const wireWorkspace = node => { node.querySelector('.store-back').addEventListener('click', () => setWorkspace('')); node.querySelectorAll('.store-item').forEach(item => item.addEventListener('click', () => { node.querySelectorAll('.store-item').forEach(x => x.classList.remove('active')); item.classList.add('active'); const name=item.textContent.replace(/\d+/g,'').trim(); if (item.dataset.storeAction === 'product') document.querySelector('#addProductBtn')?.click(); else renderFeature(name); })); };
  trigger.addEventListener('click', () => setWorkspace('store'));
  ownerTrigger?.addEventListener('click', () => setWorkspace('owner'));
  wireWorkspace(panel); wireWorkspace(ownerPanel);
  const managerProductForm=document.querySelector('#productForm');
  managerProductForm?.addEventListener('submit',event=>{
    const name=document.querySelector('#productName')?.value.trim();
    const price=Number(document.querySelector('#productPrice')?.value);
    if(!name||!price)return;
    const category=document.querySelector('#productCategory')?.value||'New Arrivals';
    const stock=Number(document.querySelector('#productStock')?.value||10);
    const preview=document.querySelector('#productImagePreview');
    const image=preview?.src&&preview.style.display!=='none'?preview.src:'https://images.unsplash.com/photo-1523381210434-271e8be1f52b?auto=format&fit=crop&w=900&q=86';
    window.setTimeout(()=>{
      if(catalog.some(product=>product.name.toLowerCase()===name.toLowerCase()))return;
      catalog.push({id:`${Date.now()}-${Math.random().toString(36).slice(2,7)}`,name,price,category,stock,image});
      saveCatalog();
      if(feature.classList.contains('open'))renderProducts();
    },0);
  },true);
  window.addEventListener('creova:product-added', event=>{const p=event.detail;if(!p?.name)return;catalog.push({id:p.id||`${Date.now()}-${Math.random().toString(36).slice(2,7)}`,name:p.name,price:p.price,category:p.category||'New Arrivals',stock:p.stock??10,image:p.image});saveCatalog();if(feature.classList.contains('open'))renderProducts();});
})();
